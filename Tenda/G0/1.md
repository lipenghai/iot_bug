## Tenda G0
> bin_vision: Tenda G0  
> httpd  
> goform/module  
>
> type: bof  
> string: portMirrorMirroredPorts  

### description
A buffer overflow vulnerability was discovered in Tenda G0. Attackers can exploit this vulnerability by specifying the value of `portMirrorMirroredPorts`. When `portMirrorMirroredPorts` is parsed by `formSetPortMirror` and written into the fixed-size stack buffer `s_` through `sprintf` without length validation, it may cause a buffer overflow.

### code

~~~c
websDefineAction("module", (int)formModules); // <--- dispatch: unified module dispatch entry
~~~

~~~c
moduleRegister("setPortMirror", (int)formSetPortMirror); // <--- dispatch: register the setPortMirror handler
~~~

~~~c
int __fastcall formSetPortMirror(int a1, int a2, int a3)
{
  const char *s; // $s0
  _DWORD *Number; // $v0
  const char *s_1; // $s0
  _DWORD *v7; // $v0
  int *String; // [sp+18h] [+18h]
  int *Bool; // [sp+1Ch] [+1Ch]
  int v10; // [sp+20h] [+20h] BYREF
  char s_[256]; // [sp+24h] [+24h] BYREF
  char s__1[64]; // [sp+124h] [+124h] BYREF
  _DWORD v13[10]; // [sp+164h] [+164h] BYREF

  v10 = 0;
  memset(s_, 0, sizeof(s_));
  memset(s__1, 0, sizeof(s__1));
  memset(v13, 0, 32);
  prod_cfm_init();
  Bool = cJSON_GetBool(a2, "portMirrorEn", 0); // <--- source: read the portMirrorEn switch
  String = cJSON_GetString(a2, "portMirrorMirroredPorts", (int)&unk_507E10); // <--- source: read the portMirrorMirroredPorts field value
  if ( String )
  {
    if ( Bool == (int *)1 )
    {
      sprintf(s_, "1;%d;%s", 3, (const char *)String); // <--- sink: source flows directly into sprintf and writes into the fixed stack buffer s_
      prod_cfm_set_val("lan.portmirror", s_);
    }
    else
    {
      GetValue("lan.portmirror", s_);
      sscanf(s_, "%d;%*d;%s", &v10, v13);
      sprintf(s_, "0;%d;%s", 3, (const char *)v13); // <--- sink: the later formatted result is still written into the fixed stack buffer s_
      prod_cfm_set_val("lan.portmirror", s_);
    }
    if ( prod_cfm_is_change() )
    {
      sprintf(s__1, "op=%d", 10);
      send_msg_to_netctrl(27, s__1);
    }
    CommitCfm();
    s = *(const char **)(a2 + 32);
    Number = cJSON_CreateNumber(0, 0);
    return cJSON_AddItemToObject(a3, s, (int)Number);
  }
  else
  {
    s_1 = *(const char **)(a2 + 32);
    v7 = cJSON_CreateNumber(-1074790400, 0);
    return cJSON_AddItemToObject(a3, s_1, (int)v7);
  }
}
~~~
